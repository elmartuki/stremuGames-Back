# 🎮 StremuGames

Bienvenido al repositorio oficial de **StremuGames-Back**.

> **🚀 Ver Proyecto Online:** [https://stremugames.vercel.app/](https://stremugames.vercel.app/)

---

## 📂 Estructura del Proyecto

El proyecto está organizado en dos carpetas principales dentro de la raíz:

* `back/`: Código del servidor (API, Base de Datos).
* `front/`: Código del cliente (React/Vite).

---

## 🛠️ Guía de Instalación y Ejecución

Sigue estos pasos para levantar el proyecto completo en tu entorno local.

### 1️⃣ Configuración del Backend

1.  **Navegar a la carpeta:**
    Abre tu terminal y entra en el directorio del backend:
    ```bash
    cd back
    ```

2.  **Configurar Variables de Entorno:**
    Busca el archivo `.env.example` dentro de la carpeta `back` y cámbiale el nombre a `.env`.
    > **Nota:** Asegúrate de que las credenciales de conexión dentro del archivo `.env` sean correctas.

3.  **Instalar Dependencias:**
    Ejecuta el siguiente comando para descargar las librerías necesarias:
    ```bash
    npm i
    ```
    
4.  **Iniciar el Servidor:**
    Levanta el backend en modo desarrollo:
    ```bash
    npm run dev
    ```
